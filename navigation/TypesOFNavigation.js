// navigate to other Screen
//  <Button title='Go to MEals' onPress={() => {
//     props.navigation.navigate({ routeName: 'CategoryMeals' })
// }} />

// it will use after login Screen
//  <Button title='Go to MEals Replace' onPress={() => {
//     props.navigation.replace('CategoryMeals')
// }} />

// // go to previous Screen
// <Button title='Go to Back' onPress={() => {
//     props.navigation.pop()
//    // props.navigation.goBack()
// }} />

// // GO to root screen
// <Button title='go to root screen'
// onPress={() => props.navigation.popToTop()}
// />


//<Button title='Go to MEals' onPress={() => {
   // props.navigation.navigate('MealDetail')
    //props.navigation.push('MealDetail') // used to go inside folder and then inside
//}} />